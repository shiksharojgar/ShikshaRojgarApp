{
  "name": "shiksha-rojgar-notifier",
  "private": true,
  "engines": { "node": "20" },
  "scripts": { "deploy": "firebase deploy --only functions" },
  "dependencies": {
    "firebase-admin": "^13.0.1",
    "firebase-functions": "^6.0.1",
    "fast-xml-parser": "^4.5.0"
  }
}
